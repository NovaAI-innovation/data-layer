# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.so

# Environments
.venv/
venv/
.env
.a0proj/secrets.env

# IDE / OS
.vscode/
.idea/
.DS_Store

# Agent artifacts (memory index is per-project; not source)
.a0proj/memory/
.a0proj/knowledge/fragments/
*.faiss
*.faiss.sha256
*.pkl

# Submodule checkout artifacts (each submodule owns its own .git/)
**/.git
**/__pycache__

# Local backups
*.tar.gz
*.bak
# === FAISS memory system (per-developer-agent runtime state, not source) ===
# All instances of the FAISS memory artifacts belong to the agent that
# built them, not to the data-layer stack. Exclude from every repo.
.a0proj/memory/
.a0proj/knowledge/
.a0proj/skills/
*.faiss
*.faiss.sha256
*.pkl
embedding.json
knowledge_import.json
